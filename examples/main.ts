import { createApp } from "vue";
import App from "./App.vue";

// import MukUI from "../packages/GsapNumber"; //导入

// import GsapNumber from "vue3-plugin-gsap-demo";
const app = createApp(App);
// app.use(MukUI); //注册
// app.use(GsapNumber);
app.mount("#app");
